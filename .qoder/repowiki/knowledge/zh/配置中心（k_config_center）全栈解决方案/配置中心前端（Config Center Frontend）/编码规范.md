- 页面按功能域拆分到 `pages/<domain>/` 子目录，每个目录包含列表页、抽屉/编辑器等视图文件
- API 调用按领域拆分为独立文件并导出统一的 TypeScript 类型，页面通过 `api/types.ts` 共享数据结构
- 可复用的表单、表格、预览、标签等 UI 抽取到 `components/`，页面只编排布局与业务逻辑
- 表格数据获取统一通过 `hooks/useTableRequest.ts` 封装分页、排序与错误处理