根目录通过 k_config_center.sln 仅聚合后端 C# 项目（k_config_center/），前端位于独立的 web/ 目录、文档位于 docs/，三者按前后端解耦方式组织：后端提供 REST API，前端通过 HTTP 调用消费；数据库 DDL 与架构设计文档集中存放于 docs/，供前后端共同参考。Solution 层面只管理 .NET 侧构建，前端由 Vite 自行构建，形成“单解决方案 + 多子工程”的松耦合结构。